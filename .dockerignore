# Dependencies
node_modules
*/node_modules
.pnpm-store

# Turbo / build cache
.turbo
dist
*/dist
build
*/build
coverage

# Frontend build artifacts
.tanstack
*/.tanstack
.vite

# TypeScript
*.tsbuildinfo

# Git
.git
.gitignore

# Editors
.vscode
.idea

# OS junk
.DS_Store
Thumbs.db

# Environment files
.env
*/.env
env/**/.env*

# Local secret overrides (docs/decisions.md #19) — never enter any build
# context, even though .env/.env.database already are excluded above;
# .env.local is a different filename those patterns don't match.
.env.local
*/.env.local

# Logs
*.log

# Docs (not needed inside the image)
docs
CLAUDE.md
