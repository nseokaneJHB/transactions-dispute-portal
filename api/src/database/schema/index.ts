// Per-entity schema files land here as docs/domain-model.md's entities are
// implemented, each re-exported below (see e.g. Ubuntu Stories'
// src/drizzle/schema for the pattern this follows — renamed src/database
// here). `drizzle-kit generate` reads this barrel to produce SQL migrations
// under src/database/migrations.
export {};
