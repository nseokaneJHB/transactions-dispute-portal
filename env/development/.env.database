# POSTGRES_USER/POSTGRES_DB aren't secret (well-known local defaults).
# POSTGRES_PASSWORD is, left blank on purpose — set via
# env/development/.env.local.database (gitignored), same value used in
# api/.env.local's DATABASE_URL. See README.md "Local setup".
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=transaction-dispute
