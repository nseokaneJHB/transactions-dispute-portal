// Barrel export for types/constants shared between `api` and `web`.
// Empty scaffold — domain types (Transaction, Dispute, DisputeReason, etc.)
// land here once the schema is decided. See docs/domain-model.md.
export {};
