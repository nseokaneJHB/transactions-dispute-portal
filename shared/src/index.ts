// Barrel export for types/constants shared between `api` and `web`.
// Domain types (Transaction, Dispute, DisputeReason, etc.) land here once
// the schema is decided. See docs/domain-model.md.

export * from "./constant.js";
export * from "./schema/field.js";
export * from "./schema/global.js";
