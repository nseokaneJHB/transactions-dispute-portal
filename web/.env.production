# Production web config. VITE_* values are read at build time and baked into
# the bundle. No secrets here (same as web/.env). Point VITE_API_URL at the
# real API origin for a production build.
VITE_APP_NAME="Transactions Dispute Portal"
VITE_API_URL=https://api.portal.example.com
